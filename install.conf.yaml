- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc

- create:
    - ~/bin

- shell:
    - [git submodule update --init --recursive, Installing submodules]
